/*
When testing, add this to your .js file
module.exports = {
    function1,
    function2,
    ...
    functionN
};
then import the functions here like:
    const { function1, function2... } = require('./mathFunctions.js');

and last, check if you get the right result:
    console.assert(function_name(paramethers) === value, 'failed');
*/